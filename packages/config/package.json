{
  "name": "@cybercrowd/config",
  "version": "0.1.0",
  "private": true,
  "description": "CyberCrowd shared configuration schemas for rooms, identity, swarm, and export behavior.",
  "type": "module",
  "main": "./src/index.js",
  "exports": {
    ".": "./src/index.js",
    "./room-config": "./src/room-config.js",
    "./identity-config": "./src/identity-config.js",
    "./swarm-config": "./src/swarm-config.js",
    "./export-config": "./src/export-config.js"
  },
  "scripts": {
    "build": "node -e \"console.log('config build complete')\"",
    "test": "vitest run",
    "lint": "node -e \"console.log('config lint placeholder')\"",
    "clean": "rm -rf dist coverage .turbo"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
